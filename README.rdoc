== JEngine

The JEngine is a rapid prototype to get a better understanding of a modeling execution environment.

== Features

This JEngine does execute
* sequential
* usertasks
* for each user

== INSTALL

* bundle install
* bin/rake db:migrate
* bin/rake db:seed 

Status: closed - running stable within v1

:-)
